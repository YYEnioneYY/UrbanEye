import { CameraMap } from '../../../features/camera-map/ui/CameraMap';

export function MapPage() {
  return (
    <section className="relative h-full overflow-hidden">
      <CameraMap />
    </section>
  );
}